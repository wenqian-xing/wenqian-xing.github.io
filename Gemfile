source "https://rubygems.org"

# Mirrors the gem versions GitHub Pages builds with.
gem "github-pages", group: :jekyll_plugins

# Not bundled with Ruby 3.0+; needed by `jekyll serve`.
gem "webrick", "~> 1.8"
